# Asset overrides

Files here are rsynced over the server's git-ignored `nitro/assets/` tree on every beta deploy (same layout: `bundled/effect/…`, `gamedata/…`). Build effect bundles with `docker/nitro/make-badge-effect.py`.
